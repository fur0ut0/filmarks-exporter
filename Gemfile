# frozen_string_literal: true

ruby '3.0.1'

source 'https://rubygems.org'

gem 'google-api-client'
gem 'redis'

group :development, :test do
   gem 'dotenv'
   gem 'overcommit'
   gem 'pry'
   gem 'solargraph'
end
